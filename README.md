# SmartScale
Code for a BLE-enabled esp32c3 microcontroller and a Python app which runs on the computer the device is paired with.
Connect a load cell and load cell amplifier to the MCU and calibrate the load cell. The app takes JSON recipe files (example BananaBread.json included here) and proceeds through the recipe, moving forward when the correct amount of the ingredient in question is added.
